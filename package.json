{
  "name": "automix-youtube-ai",
  "version": "1.2.0",
  "private": true,
  "description": "AutoMix + multi-channel YouTube uploader + AI + persistent recovery scheduler + mobile PWA + Render/Postgres",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node scripts/check.js",
    "test": "node scripts/self-test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@ffmpeg-installer/ffmpeg": "^1.1.0",
    "@ffprobe-installer/ffprobe": "^2.1.2",
    "compression": "^1.8.1",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "googleapis": "^173.0.0",
    "helmet": "^8.1.0",
    "pg": "^8.13.1"
  }
}
