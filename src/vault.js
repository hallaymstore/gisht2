const crypto=require('crypto'); function key(){const secret=process.env.APP_SECRET||'';if(secret.length<24)throw new Error('APP_SECRET kamida 24 belgidan iborat bo‘lishi kerak');return crypto.createHash('sha256').update(secret).digest();}
function encryptJson(value){const iv=crypto.randomBytes(12);const cipher=crypto.createCipheriv('aes-256-gcm',key(),iv);const payload=Buffer.from(JSON.stringify(value),'utf8');const encrypted=Buffer.concat([cipher.update(payload),cipher.final()]);const tag=cipher.getAuthTag();return[iv,tag,encrypted].map(x=>x.toString('base64url')).join('.');}
function decryptJson(payload){if(!payload)return null;const[ivText,tagText,encryptedText]=String(payload).split('.');if(!ivText||!tagText||!encryptedText)throw new Error('Shifrlangan token formati yaroqsiz');const decipher=crypto.createDecipheriv('aes-256-gcm',key(),Buffer.from(ivText,'base64url'));decipher.setAuthTag(Buffer.from(tagText,'base64url'));const decoded=Buffer.concat([decipher.update(Buffer.from(encryptedText,'base64url')),decipher.final()]).toString('utf8');return JSON.parse(decoded);} module.exports={encryptJson,decryptJson};
