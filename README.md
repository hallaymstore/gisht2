# AutoMix YouTube AI v1.2

AutoMix video generator + multi-channel YouTube uploader + AI metadata + recovery scheduler + mobile PWA.

This build includes Render/PostgreSQL state persistence support. See `README_UZ.md`, `MOBILE_REMOTE_UZ.md` and `RENDER_DEPLOY.md`.

> Never commit `.env` or OAuth/API secrets.
